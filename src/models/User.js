// User model — defines the shape of a user in the database
// Replace with your DB schema (Mongoose, Sequelize, Prisma, etc.)

const User = {
  // id, name, email, password, role, createdAt
};

module.exports = User;
