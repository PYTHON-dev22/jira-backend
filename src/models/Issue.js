// Issue model — defines the shape of an issue (task/bug/story) in the database

const Issue = {
  // id, title, description, status, priority, type, projectId, assigneeId, reporterId, createdAt
};

module.exports = Issue;
